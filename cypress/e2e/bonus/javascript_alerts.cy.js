describe("JavaScript Alerts Tests", () => {
    beforeEach(() => {
      cy.visit("/javascript_alerts");
    });

    it("should handle an alert box", () => {

    });

    it("should handle a confirmation box (OK)", () => {

    });

    it("should handle a confirmation box (Cancel)", () => {

    });

    it("should handle a prompt box", () => {

    });
  });
